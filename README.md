# Klexir.EventFlow

Strongly typed event bus for the Klexir ecosystem.

Only `Klexir.EventFlow.Abstractions` is a public NuGet package. The first increment provides an in-memory bus with deterministic sequential dispatch; resilience, persistence and distributed adapters follow in separate milestones.
